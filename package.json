{
  "name": "@flashfix/express-trailingslash",
  "description": "ExpressJS middleware that redirects requests with trailing slashes to a clean one without it",
  "author": "flashfix <max@flashfix.app>",
  "license": "MIT",
  "readmeFilename": "README.md",
  "repository": "flashfixapp/express-trailingslash",
  "bugs": "https://github.com/flashfixapp/express-trailingslash/issues",
  "version": "1.0.2",
  "keywords": [
    "express",
    "expressjs",
    "middleware",
    "slash",
    "trailing"
  ],
  "main": "index.js",
  "scripts": {
    "reinstall": "rm -rf node_modules package-lock.json && npm install",
    "lint": "eslint ./",
    "prepublishOnly": "npm test && npm run lint && echo \"\\033[0;32mReady to publish\\033[0m\"",
    "test": "TZ='GMT' mocha"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^8.9.0",
    "eslint-config-flashfix-base": "^1.0.1",
    "mocha": "^9.2.2",
    "sinon": "^13.0.1"
  },
  "dependencies": {}
}
